
def high(x):


    # for word in x.split(" "):
    #
    score = (sum(ord(ch) - 96 for ch in (for word in x.split(" "))))
    #
    #         print(word, score)
    #
    # return highest

print(high('time what are we climbing up the volcano'))
