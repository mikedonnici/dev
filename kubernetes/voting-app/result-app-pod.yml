apiVersion: v1
kind: Pod
metadata:
  name: result-app
  labels:
    name: result-app
    tier: frontend
spec:
  containers:
    - name: result-app
      image: kodekloud/examplevotingapp_result:v1
      ports:
        - containerPort: 80
